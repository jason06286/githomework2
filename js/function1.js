function multiplication(params) {
    
    for (let i = 3; i < 16; i++) {
        for (let l = 1; l < 16; l++) {
            let str= `${i}*${l}`
        console.log(str)
        }
    }
}
multiplication()